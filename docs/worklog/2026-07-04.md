# 2026-07-04 (토) 워크로그

> /morning이 생성하고 /evening이 마감한다. 섹션 제목을 바꾸지 말 것
> (SessionStart 훅이 "내일 할 일" 섹션을 파싱해 다음 세션에 자동 주입한다).

## 오늘 목표

- [x] TODO 조회/정렬 API: GET /todos?date=, PATCH /todos/reorder (TECH_DESIGN §3)
- [x] 관리자 TODO CRUD: POST/PATCH/DELETE /admin/todos (TECH_DESIGN §3)
- [x] ★ 토글 서비스: 토글 ON (TECH_DESIGN §4.1) — 미션 +5, 완주 +10, 연속 +10×(N−1), daily_bonus_log
- [x] ★ 토글 서비스: 토글 OFF (TECH_DESIGN §4.2) — 정확 회수 + streak 되돌림
- [x] ★ TECH_DESIGN §6 시나리오 테스트 1~3 (미션/보너스/회수)

## 진행 기록

- GET /todos 소유자 해석: 2인 구조라 `services/users.resolve_todo_owner`로 대상을 정한다 — 사용자=본인, 관리자=유일한 role='user' 계정. 관리자도 사용자 목록을 조회(SPEC §5)해야 하는데 §3 계약에 user_id 파라미터가 없어 이렇게 해석. e2e로 관리자가 사용자 TODO를 보는 것 확인.
- reorder는 사용자 전용 + 본인의 '오늘' TODO만 + 전부-아니면-거부(부분 적용 방지). 요청 id 중 하나라도 본인/오늘이 아니면 400.
- 토글 포인트 로직은 전부 `services/toggle.py`. A·B 동시 기록은 `_record_both`, 원장 기록/잔액 캐시 갱신은 `services/points.record_point` 한 곳으로 (불변 규칙: 잔액만 갱신 금지). 회수는 `clamp_nonneg=True`로 §4.2 완화 규칙(0까지만 회수, 원장엔 실제 회수액) 반영 → 잔액-원장 항상 일치.
- 완주 중복 지급 방어: 완주 판정 후 daily_bonus_log 행이 이미 있으면 보너스 skip (관리자가 완주 후 TODO를 추가하는 등의 재완주 시 이중지급 차단).
- 라우터에서 잔액 필드를 직접 만지면 post-edit 훅이 불변규칙 위반으로 막는다 → balances를 ToggleResult에 담아 서비스가 반환하도록 수정. 라우터는 얇게 유지.
- Dockerfile이 런타임 의존만 설치해 컨테이너에 pytest/ruff가 없었다(어제 테스트는 호스트에서 돌린 듯). CLAUDE.md의 `docker compose exec api pytest -q`가 바로 되도록 Dockerfile을 `-e ".[dev]"`로 변경 후 재빌드 → 문서화된 명령이 즉시 동작 확인.

## 완료한 일

- `schemas/todo.py`: TodoOut, Reorder(Item/Request), AwardItem, ToggleResponse, AdminTodoCreate/Update.
- `services/users.py`: get_the_user / resolve_todo_owner (2인 구조 대상 해석).
- `routers/todos.py`: GET /todos?date=(기본 논리적 오늘), PATCH /todos/reorder(사용자·오늘·원자적), PATCH /todos/{id}/toggle.
- `routers/admin_todos.py`: POST(sort_order=max+1 뒤에 추가) / PATCH(부분수정) / DELETE. 관리자 전용, 순수 CRUD.
- `services/points.py`: record_point — 원장 1행 + 잔액 캐시, clamp_nonneg 완화 규칙, 0원 거래 미기록.
- `services/toggle.py`: toggle_todo(ON/OFF 자동 분기) + _toggle_on/_toggle_off/_all_done_that_day/_record_both. §4.1/§4.2 그대로.
- main.py에 todos·admin_todos 라우터 등록.
- `tests/test_toggle.py`: §6 시나리오 1~3 + 부분완료/부분취소 = 7개. 매 검증에서 잔액-원장 합계 일치 확인. **총 20 pass, ruff clean**.
- Dockerfile dev 의존 설치 + 재빌드. e2e: 로그인→관리자 TODO 생성→GET/토글 ON(15/15,streak1)/OFF(0/0,streak0)→관리자 토글 403→삭제 204까지 실제 HTTP로 확인. dev DB 정리 완료.

## 이슈 / 배운 것

- **미해결 설계 공백(§4 범위 밖)** → **오후에 사용자 결정으로 해소**: "관리자가 완주 이후 TODO 편집" 문제를, 사용자가 "완주 보너스를 하루 경계(하루 마감)에 한 번만 판정·지급"으로 정리. 즉시 지급을 하루 끝으로 미루니 미리 준 보너스를 회수할 왕복 자체가 사라짐. → 아래 "설계 변경" 참조.
- done_at은 UTC 저장. 로컬 07-04 01:33(KST)이 UTC 07-03 16:33로 저장됨 — 논리적 날짜(07-04)와 별개로 정상. 표시 변환은 클라이언트 몫.
- 어제 테스트가 컨테이너 밖(호스트)에서 돌던 흔적 → 이제 컨테이너 내 표준화.

## 설계 변경 — 완주 보너스 지급 시점 (오후, 사용자 확정)

- **결정(사용자)**: 미션 +5는 토글 즉시 유지, **완주·연속 보너스는 하루 경계(하루 마감) 시점에 판정·지급**. "11:59:59" 요청은 CLAUDE.md 불변규칙(자정 하드코딩 금지)과 충돌 → **day_boundary_time 연동**으로 확정(기본 00:00=자정, 09:00이면 다음날 09:00).
- **문서 우선 갱신**: SPEC §2.2/§2.3/§6.1.1 + 부록 B, TECH_DESIGN §4.1(토글=미션만)/§4.2(미션 회수만)/신규 §4.6(하루 마감 크론)/§3 API에 `POST /internal/close_day`/§6 시나리오 재정의.
- **구현**:
  - `services/toggle.py` 축소 — 완주/streak/daily_bonus_log 블록 제거, 미션 ±5만. `record_both`는 `services/points.py`로 승격(토글·마감 공용).
  - `services/day_close.py` 신규 — `close_day(db, target_date=None)`(기본 어제) / `close_day_for_user`. 완주 판정→완주+연속 보너스·streak·daily_bonus_log. **멱등**: daily_bonus_log (user_id,due_date) 있으면 skip. 미완주/0개→streak 0 리셋(단 target_date>last_complete일 때만 리셋 — 오래된 날 재실행이 이후 streak 덮어쓰는 것 방지 가드).
  - `routers/internal.py` 신규 — `POST /internal/close_day`, `X-Cron-Secret` 헤더 검증(settings.cron_secret). date 쿼리 옵션.
- **테스트**: test_toggle.py 축소(미션만·완주시점 보너스 없음·과거 403), test_day_close.py 신규(N=1·2·3, 미완주 리셋, 0개 끊김, **멱등 2회**, streak_reset_days, 오래된 미완주 가드). **총 25 pass, ruff clean**.
- **e2e**: 토글 ON=미션만(5/5) → close_day 무시크릿 403 → 시크릿 있음 완주(streak1, +10) → /me a=15 → 재실행 already_closed(이중지급 없음). dev DB 시드 상태로 리셋 완료.

## Phase 2 — 사용자 앱 + 관리자 화면 (저녁, 웹으로 확인)

- **동기**: 사용자가 "웹서버로 볼 수 있냐"고 물음 → 지금까진 백엔드뿐이라, 눈에 보이는 사용자 앱을 착수.
- **스캐폴딩**: `app/` Expo SDK 57 blank-TS. 백엔드 CORS 미들웨어 추가(main.py, 웹 클라이언트용). 의존: react-native-svg(진행 링), @react-native-async-storage(토큰), @expo-google-fonts/jua·noto-sans-kr(폰트).
- **디자인 방향(frontend-design 스킬)**: "보상 기계" 컨셉. AI 기본값(크림+세리프/블랙+형광/신문) 회피. 팔레트: 따뜻한 아이보리 + A핑크/B민트로 두 지갑 색분리 + 연속 amber. 폰트 Jua(디스플레이)+NotoSansKR(본문). 시그니처 = 완료할수록 차오르는 진행 링 + 포인트 팡.
- **사용자 앱**: LoginScreen, TodayScreen(지갑 칩 2 + 진행 링 + 미션 카드 토글 + PointPop). 서버 awarded/balances만 반영(자체 계산 금지). 완주 시 "완주 보너스는 하루 마감 때" 정직 안내(하루-경계 규칙과 일치).
- **관리자 화면**: 역할 분기(App.tsx가 /me role로 Today/Admin 라우팅, SPEC §10). AdminScreen(미션 추가/삭제, 사용자 현황 요약, 민트 톤). 백엔드 `GET /admin/user`(관리 대상 사용자)·`GET /today`(논리적 오늘) 추가.
- **검증(헤드리스 Edge + puppeteer-core)**: tsc 통과, 웹 번들 393모듈 에러0. 로그인→오늘 렌더, 미션 4개 토글→지갑 20/20·링 4/4·"다 끝냈어요" 배너 확인(스크린샷). 관리자 로그인→미션 관리 렌더, 추가 POST 201 확인. 런타임 에러 없음(무해한 RN-web collapsable 경고만).
- **실행법**: 백엔드 `docker compose up -d`(api :8001) + 앱 `cd app && npx expo start --web`(:8081). 데모 계정 user/user1234, admin/admin1234. 데모 미션 4개 시드됨.

## Phase 3 착수 — A상점 구매 API (저녁, §6-5)

- **커밋**: 사용자 요청으로 오늘 작업 일괄 커밋 `0c48e07` (완주 재설계 + TODO/토글 API + Phase 2 앱). 이후 A상점은 별도 미커밋.
- **구매 서비스**(services/shop.py): purchase_item — A포인트만 차감(record_point kind 'A'), B 미접촉(불변규칙), 옷장 추가까지 단일 트랜잭션. 사용자 전용(403), 없음/비활성 404, 이미 보유 400, A부족 400(무변화).
- **라우터**(routers/shop.py): POST /shop/purchase, GET /shop/items?category=, GET /inventory(인벤토리+아이템 조인).
- **테스트**(test_shop.py) §6-5: A만 차감·B불변·원장 shop_purchase 1행·B행 없음 / 부족 400 무변화 / 이미보유 400 / admin 403 / 없음·비활성 404. **총 31 pass, ruff clean**.
- **e2e**: user 로그인→GET items→구매(A 20→10, B 20 유지)→부족 400→중복 400→GET inventory→/me a=10. dev DB 사용자 상태 복원.
- **주의(배운 것)**: `docker compose exec python -c "…한글…"` 인라인은 Windows shell에서 한글 바이트가 깨진다(shop 아이템명 깨짐 확인). 실제 카탈로그 시드는 app/seed.py 같은 UTF-8 .py 파일로 넣어야 함. (앞서 TODO 시드가 됐던 건 운이 좋았던 케이스 — 신뢰 불가)

## A상점 UI (밤)

- 커밋 `2c58cb0`(A상점 API) 후 UI 진행.
- `seed_shop.py`: 카탈로그 17개 멱등 시드. 이미지 에셋 전이라 `emoji:🎀` 규약(클라이언트가 이모지 렌더). 한글 시드는 반드시 UTF-8 .py로.
- `ShopScreen`: 카테고리 그룹 그리드, 보유중(민트 뱃지·흐림), 잔액부족(회색 가격), 구매 시 서버 balances만 반영 + `-가격` 팡. A지갑만 노출(B는 상점과 무관 — 차감 독립을 화면에서도 분리).
- `App.tsx`: 사용자 role에 하단 탭(오늘 ✅/상점 🎀) 추가. 탭 전환 시 재마운트로 잔액 동기화.
- 검증: tsc 통과, 헤드리스로 로그인→상점 탭→리본 머리핀 구매(지갑 20→10, 보유중 전환) 확인, 에러 0. 테스트 구매는 원복(a=20, 옷장 비움).

## 상점 UX 개편 (밤 2차, 사용자 요청)

- 하단 탭 제거 → **오늘 화면의 A·아바타 카드 탭 = 상점 진입**, 상점 좌상단 ← = 복귀 (UserFlow).
- **착용 미리보기**: 아이템 탭 → 우측 "내 아바타" 패널에 착용(카테고리당 1개, 재탭 해제, 배경은 패널 배경으로). 카드에 핑크 테두리+"착용중" 마크.
- **구매 확인**: 착용 중 미보유 아이템 목록+합계 → 구매하기 → **"구매하시겠습니까?" 모달 → "네"를 눌러야 차감** (RN Modal, 웹 호환).
- 헤드리스 검증: 진입→착용→해제→재착용→모달→네→지갑 50→38·보유중 전환. 에러 0.

## ★ 포인트 일괄정산 재설계 (밤, 사용자 버그 리포트로 확정)

- **버그(실사용 발견)**: 실시간 미션 +5 → 상점 소비 → 토글 OFF(잔액 0이라 회수 불가·완화규칙) → 재완료 → +5 재지급 = **무한 생성**. 사용자가 직접 재현.
- **새 규칙(사용자 결정)**: 토글은 잔액·원장 **완전 불변**(완료 표시만). **미션+완주+연속 전부 하루 마감에 합산 일괄 지급**. 낮에는 "들어올 예정 포인트"를 실시간 표시(서버 계산).
- 문서: SPEC §2.2/§6.1.1/부록B, TECH_DESIGN §4.1/§4.2/신규 §4.2.1(pending)/§4.6/§2 DDL/§3/§6 개정.
- 마이그레이션 `29ad3da1c7d0`: daily_bonus_log.mission_points 추가. 이제 **마감한 날마다 행**(완주 무관) = 전일 멱등 키.
- 백엔드: toggle.py 포인트 코드 전부 제거. day_close.py가 미션 합계까지 지급 + compute_pending(§4.2.1). GET /points/pending, 토글 응답 awarded→pending 교체.
- 테스트 재작성: **토글 스팸(ON→OFF×5) 후 원장 0행** 검증 포함, 마감 멱등(부분완료 날 포함), N=1·2·3, reset_days, 가드. **총 33 pass, ruff clean**.
- e2e: 토글 내내 잔액 0 고정, pending 35↔20 실시간. UI: 지갑 아래 점선 pending 카드("다음 00:00에 들어와요 (현재 기준) +35P" + 미션·완주·연속 내역), 완주 배너 문구 갱신.
- dev 상태: 어제 정산분 시뮬레이션 50P 지급(원장 포함) → 사용자가 상점 테스트 가능 (현재 a=38).

## 내일 할 일

- **미커밋**: 상점 UI/UX 개편 + 포인트 일괄정산 재설계 전체(백엔드·앱·문서·마이그레이션). 요청 시 커밋.
- **로컬 하루마감 자동화 없음 주의**: 크론 미연결(Phase 6)이라 00:00에 실제 지급은 안 됨. 임시로 /morning에서 전일 close_day 호출하는 방안 검토.
- 옷장/착용 equip API + §6-6 테스트 (구매한 아이템을 실제로 착용 저장 — 지금 미리보기는 클라이언트 전용).
- 아바타 룸(SPEC §3.3) 또는 B상점 화면.
- 드래그앤드롭 정렬 UI (PATCH /todos/reorder 백엔드 준비됨) — Phase 2 마지막 조각.
- 실제 크론 스케줄러 연결은 Phase 6. 로컬에서 하루 마감 자동 호출 임시방편 필요 시 검토.
- 네이티브 기기에서 열려면 app/src/api.ts의 API_BASE를 PC LAN IP로 교체 필요(현재 localhost = 웹 전용).
- (또는) A상점 구매 API + §6-5 테스트로 포인트 '차감'(shop_purchase) 원장 경로.
