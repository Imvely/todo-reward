# 2026-07-03 (금) 워크로그

> /morning이 생성하고 /evening이 마감한다. 섹션 제목을 바꾸지 말 것
> (SessionStart 훅이 "내일 할 일" 섹션을 파싱해 다음 세션에 자동 주입한다).

## 오늘 목표

- [x] docker-compose.yml 작성 (FastAPI + PostgreSQL 16, TECH_DESIGN §1)
- [x] backend 폴더 구조 생성 (TECH_DESIGN §5) + FastAPI 엔트리 + ruff 설정
- [x] (여력) Alembic 초기화 + 전체 스키마 마이그레이션 (TECH_DESIGN §2, pg_trgm 포함)
- [x] (추가) 시드 스크립트 + 하루 경계 헬퍼 + 인증(/auth/login, /me) — 원래 내일 몫을 당겨서 완료
- [ ] 색상 테마·목업 방향 — 사용자 결정 대기 (브리핑 질문 참조)

## 진행 기록

- 첫 작업일. 레포에 docs만 존재, 백엔드 전무 → 그린필드로 Phase 1 시작.
- 호스트 5432/8000이 다른 로컬 프로젝트(readmind-postgres, ocr-ingest-api)에 점유됨 → compose 호스트 포트를 db 5434, api 8001로 변경. 컨테이너 내부 포트와 `db:5432` 접속은 그대로.
- 스키마는 SQLAlchemy 2.0 모델(도메인별 파일) → alembic autogenerate → pg_trgm 확장 + settings 싱글턴 INSERT 수동 추가.
- passlib이 최신 bcrypt와 비호환(초기화 시 72바이트 자체검사 ValueError) → passlib 제거하고 `bcrypt` 직접 사용으로 전환.
- 인증 흐름 순조로워 원래 내일 예정이던 시드·경계 헬퍼·인증까지 오늘 당겨서 완료.

## 완료한 일

- `docker-compose.yml` (db: postgres:16 healthcheck, api: uvicorn --reload, depends_on healthy).
- `backend/` 뼈대: Dockerfile, pyproject.toml(ruff+deps), app/{core,models,schemas,services,routers,crons}, tests, .env.example, .dockerignore.
- `app/core/config.py`(pydantic-settings), `app/core/db.py`(engine/SessionLocal/Base/get_db), `app/main.py`(/health).
- 전체 스키마 모델 12테이블 (users, settings, todos, routines, point_transactions, daily_bonus_log, shop_items, user_inventory, settlements, coin_transactions, badges, user_badges) — CHECK/UNIQUE/FK, 부분 유니크 인덱스 one_equipped_per_cat, 트라이그램 인덱스 idx_todos_content_trgm.
- Alembic 초기 마이그레이션 `52ebf0c5fbb2` 적용 + 왕복(downgrade base→upgrade head) 검증, `alembic check` 무변화 확인.
- Alembic·스키마 검증: ruff check/format 통과, DB에서 테이블·확장·싱글턴 행·인덱스 실물 확인.
- `app/seed.py`: 관리자/사용자 멱등 시드 (env 기본값 admin/admin1234, user/user1234). 실제 실행 + 재실행 skip 확인.
- `app/services/day_boundary.py`: `logical_date(now, boundary_time, tz)` + `current_logical_date(db)`. SPEC §2.3 의미론(경계 이전=어제). 순수 단위 테스트 7.
- 인증: `core/security.py`(bcrypt 직접, JWT), `core/deps.py`(HTTPBearer→get_current_user/require_admin), `routers/auth.py`(/auth/login), `routers/me.py`(/me), 스키마. `/api/v1` 접두사.
- 테스트: 총 13 pass (health 1 + 경계 7 + 인증 5). 인증 통합 테스트는 커넥션 롤백 픽스처(conftest.py)로 dev DB 무오염.
- e2e: uvicorn 기동 후 시드 admin으로 실제 로그인→JWT→/me 확인, 토큰 없음/오답 비번 각각 401.

## 이슈 / 배운 것

- passlib 1.7.x + bcrypt 4.x 비호환 → bcrypt 직접 사용. 앞으로 해시는 `app/core/security.py` 한 곳만 사용.
- httpx TestClient StarletteDeprecationWarning 발생하나 무해 (테스트 통과). 추후 httpx 버전 정리 시 확인.
- 인증 테스트는 DATABASE_URL(→localhost:5434)이 설정돼야 실행됨. 순수 테스트(health·경계)는 DB 불필요.
- 아직 미커밋. 커밋은 /evening 또는 사용자 요청 시.

## 내일 할 일

- 하루 경계 헬퍼를 실제로 소비할 첫 지점: TODO 조회/정렬 API (GET /todos?date=, PATCH /todos/reorder, TECH_DESIGN §3).
- 관리자 TODO CRUD: POST/PATCH/DELETE /admin/todos (§3).
- 그 다음 큰 산: ★ 토글 서비스 ON/OFF (TECH_DESIGN §4.1/§4.2) + §6 시나리오 테스트 1~3.
